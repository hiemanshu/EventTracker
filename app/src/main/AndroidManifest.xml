<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.theindiangeek.eventtracker" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.activities.LoginActivity"
            android:label="EventTracker" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.EventListingActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".ui.activities.EventViewActivity"
            android:label="@string/title_activity_event_view" >
        </activity>
        <activity
            android:name=".ui.activities.TrackedEventsActivity"
            android:label="@string/title_activity_tracked_events" >
        </activity>
    </application>

</manifest>
